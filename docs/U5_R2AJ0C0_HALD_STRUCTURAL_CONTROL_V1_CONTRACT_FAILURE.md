# U5.R2AJ0C0 v1 — pre-execution contract failure

## Result

`U5.R2AJ0C0` v1 is closed before implementation or formal execution. No
archive was opened, no primary CLUT body or metric was read, no photograph
was rendered and no evidence was promoted.

The frozen files remain unchanged at commit `cdd20cd`:

- config SHA-256:
  `a75d0d2282a0a88b2427d14562bc88151c5dbbd0d2ee8ef8023620fe73d48824`;
- contract SHA-256:
  `c14c920efe1dc1f58cc213721f6210a99bbe15470ce0e682020bc856aad4c6be`.

## Blocking review findings

1. The contract called `17` and `33` native Hald cube sides even though legal
   Hald cube side is `N=L^2`; those values are metric probe populations, not
   square-raster Hald geometries.
2. The hard-clip and staircase negative-control formulas did not fully fix
   boundary inclusion, quantization order or the meaning of level count.
3. Byte-identical reports alone did not bind strict schemas, config and
   software identities, ordered control hashes, runtime, parent evidence,
   access counters and single-run non-promotion.
4. Cool component separation did not require a positive warm-versus-cool
   residual-novelty distance, so a broken novelty implementation could pass.
5. The future cap-12 tie break referred to an undefined normalized safety
   margin and did not define an intra-strength-component representative.

These are contract defects, not algorithm results. V1 thresholds are not
being changed in response to a primary candidate: no primary metric has been
observed.

## Branch

The only allowed next leaf is separately versioned `U5.R2AJ0C0B`. It must:

- distinguish probe grids from legal Hald raster/cube geometry;
- freeze exact negative formulas;
- emit strict manifest, report and repeat-decision evidence;
- bind warm/cool novelty explicitly;
- defer the incomplete cap-12 selection details to the later C1 contract.

`U5.R2AJ0C1`, photography, aesthetics, fitting, training, LSM and integration
remain forbidden.
