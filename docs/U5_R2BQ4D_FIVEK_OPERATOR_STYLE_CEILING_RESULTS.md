# U5.R2BQ4D operator style ceiling

Two development-only diagnostic runs are byte-identical at SHA-256
`a3949faa1a2f2c37de18fcd4cbcdeb0404a238e3de0b395266f07c1189052888`.
No fitting, training or confirmation loading occurs; the experiment only
replays the already-fitted 14-parameter case bank through the established
analytical boundary-safe executor.

The current per-image fit is the bottleneck. Even the operator fitted to its
own target retains only 43.84% of Aligned Expert style and 58.52% of Filtered
style at the median, both below the frozen 70% requirement. Error-minimizing
group-held Oracle retention is 69.65%/80.48%.

The evidence does not say the data lack strong style. Every held row has at
least one safe fit-group case above 70% retention; the strongest-case medians
are 2.00x/2.61x target style. Constraining the evaluator Oracle to those strong
cases raises mean error only 3.62%/1.97%, and new boundary remains zero.

Per the frozen branch, the current triangular fit/operator family closes rather
than receiving a router or loss-function rescue. The next experiment reuses the
already-established strict-interior parallel projection-curve operator as an
unchanged mature capacity baseline on this larger case bank. This isolates
renderer capacity before any source-only selector is trained. All evidence is
FiveK digital-retouch control evidence, not film or product proof.
