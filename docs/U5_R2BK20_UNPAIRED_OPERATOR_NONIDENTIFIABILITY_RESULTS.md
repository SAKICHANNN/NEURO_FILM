# U5.R2BK20 Unpaired Operator Nonidentifiability Results

Two byte-identical formal runs produced SHA-256
`340d31d041fe94e80b97cdc8ea11a3d2b79496b7653c48bd0ebf52c15c449970`.
The constructive witness passes every frozen gate.

The identity world and a smooth z-conditioned angular-twist world expose the
same 504-colour source and target sets after canonical sorting, all at SHA-256
`9778578d239814230391a4e2a87e846fbaa70c3ede691b56851bca2df10cd39b`.
Their actual paired transforms are nevertheless materially different:

- paired RGB RMSE: `0.063596`;
- residual RMSE after best affine basic adjustment: `0.059003`;
- twist inverse maximum error: `0`;
- out-of-cube fraction: `0`.

Lexicographic-rank and minimum-Euclidean assignment both recover identity and
recover zero true twist rows because the unpaired observations contain no
information that distinguishes the two worlds.

This is a constructive synthetic boundary, not evidence against useful
reference matching. It means reference-only output must be described as
film-inspired appearance matching, with canonicalizer/control-pool sensitivity
and hard fallback, unless independent paired evidence identifies the operator.
