# U6.P2W Langmuir interimage photographic results

The fixed clean-room Langmuir donor is visibly nonempty on all 16 frozen
rights-cleared photographs. Two complete 49-file runs are byte-identical at
`500ae294...22bf`; the stable evidence ID is `b693e2cc...8a95`.

The population median Langmuir-versus-linear effect is `.7351` Delta E76 and
the largest per-image median is `1.4877`. Median style and non-basic residual
versus input are `16.6934` and `10.1660`; new hard-boundary fraction is zero,
median absolute luma shift is `.00481`, median Lab chroma ratio is `.98793`,
and gradient-energy ratios stay within `1.0533-1.1256`.

The automatic decision is nevertheless a hard close. `canon_eos_kiss_f`
contains 283 isolated excursions against the frozen requirement of zero. This
is the sole failed gate, so visual review was forbidden and was not performed.
No same-cohort strength/coupling/placement search, safety-scale smoothing or
threshold relaxation is allowed.

U6.P2V remains valid synthetic mechanism evidence, while this corrected
log-exposure photographic realization is rejected. A distinct, separately
preregistered mechanism may test an intrinsically bounded saturating response
without per-pixel post-hoc scaling. Nothing here identifies chemistry, a film
stock, process, scanner response, calibration or a product operator.
