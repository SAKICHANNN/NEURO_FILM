# U5.R2REPID9D — CanonCGT grade-embedding operator predictor

Date: 2026-08-20

REPID9C closed the exact global reference-statistics observation while leaving
a substantial paired per-scene operator ceiling. REPID9D changes only that
identifying observation: the fixed official CanonCGT E2E checkpoint emits its
normalized 64D `Embedding_Net` style token for each winner reference. No model
weight is trained or fine-tuned.

The 80/24 roles, safe 12D logit-affine labels, ridge alpha, parameter bounds,
dose grid and scientific gates remain fixed. Equal-ridge raw statistics, the
global mean effect and cyclic parameter labels are mandatory controls. All 24
candidate/control predictions freeze before any calibration original read.

The calibration rows are now consumed development, never fresh confirmation.
Any gate failure closes the exact embedding/checkpoint/layer/ridge/operator
family without crop, resolution, layer, alpha or threshold rescue. Only an
all-gate pass may open the untouched 24-scene sealed role under a new contract.
