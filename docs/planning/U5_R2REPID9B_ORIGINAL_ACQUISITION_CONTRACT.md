# U5.R2REPID9B — REPID original acquisition contract

Date: 2026-08-20

REPID9B downloads exactly the 104 original members frozen by REPID9A into an
explicit CLI-provided project fallback root. P is not a write destination while
its exFAT volume reports repair required. No sealed member is requested.

Every final file must match its frozen byte count and LFS SHA-256, decode as
JPEG/RGB with minimum dimension 256, and retain a recorded ICC identity without
interpreting it. Forward and reversed enumeration must produce identical
normalized reports; the second run must safely resume the exact files. A pass
opens only the separately frozen reference-only bounded-parameter predictor D0.

Execution amendment: the first formal attempt produced no report after a
transient TLS EOF. The scientific identities, roles, byte hashes and gates are
unchanged. Acquisition now retries transport-only failures after fixed
0.5/1/2/4 second delays; identity and decode failures remain immediate errors.
