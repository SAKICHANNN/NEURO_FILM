# U5.R2REPID9A — REPID original-member lock contract

Date: 2026-08-20

REPID6-8 show that reference-only inference needs an identifying observation
stronger than a synthetic atlas or free inverse capacity. REPID9 changes the
information source: professional same-scene original/render relations may
supervise a small reference-only predictor of bounded explicit-operator
parameters. At inference the predictor will still read only the reference;
the application source remains absent from Build.

Before any original member payload is requested, REPID9A binds the already
frozen REPID2 80 fit and 24 calibration scenes to their exact
`images/original/{scene_id}` paths, sizes and LFS SHA-256 identities. A bounded
metadata feasibility query was already observed before this contract: all 104
paths resolved and total 10,582,439 bytes, while payload reads remained zero.
This prospective adoption records that observation rather than pretending it
was unseen. Every row is mandatory and replacement is forbidden.

The builder must reproduce the same canonical manifest in forward and reverse
role enumeration, resolve all 104 paths with LFS hashes and JPEG extensions,
remain below 32 MiB, and keep sealed requests, member reads, image decodes and
operator fits at zero. A pass opens only exact fit/calibration original-member
acquisition into the project-isolated D fallback while P requires repair.
