# U5.R2AO5 combined Velvia result

The fixed one-matrix model fitted to all 71 exact Velvia display-proxy pairs
passes twice exactly at `fa20ed44...7bd2`. It reaches `.02941` combined RMSE,
76.47% below identity and 40.51% below full-affine. Chart and pigment subsets
remain at `.04121` and `.02096`; all one-matrix predictions are in-cube.

The unchanged AO3 factor bank leaves one photo survivor:

- tone/chroma `.50/1.00`;
- gold style/non-basic `8.4672/5.0243`;
- zero new gold and stress clipping;
- exact 164-output manifests at `8084d22c...45a6`;
- exact automatic reports at `3c745571...27bf`.

Three distinct blind layouts rank the combined proxy above the chart-only proxy
in 3/3 rounds, confirming that the added pigment evidence reduces the
systematic cool/grey bias. The vivid retained B0 comparator still ranks above
it in 3/3. Full-resolution review of all nine gold images finds zero confirmed
severe failures, including no ID11 red speckle, face corruption, posterization,
banding or boundary pileup.

Retain the combined operator as the strongest real-film display-proxy algorithm
evidence and a genuine direction improvement, not the aesthetic champion. The
red response midpoint still reaches its optimizer bound, and all data share one
paper/rendering pipeline. A next composition leaf may test whether a small
amount of this real-film direction can discipline the vivid B0 density path
without averaging away its style.
