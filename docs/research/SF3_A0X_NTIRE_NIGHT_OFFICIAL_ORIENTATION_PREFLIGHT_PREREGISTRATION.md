# SF3.A0X official-orientation geometry preflight preregistration

Date: 2026-08-21

SF3.A0X is the one and only prospective successor permitted after SF3.A0W.
It preserves A0W's failure, exact eight development IDs, archive identities,
official geometry, SIFT/RANSAC parameters, absolute gates and wrong-target
control. The only change is to implement the full orientation mapping already
published in the pinned official MIT baseline before A0W was designed.

The overlay contract binds the exact A0W base-contract SHA and enumerates all
eight official string-to-EXIF mappings plus numeric values 1 through 8. It does
not select orientation from pixels. Any further metadata, decode, geometry or
registration mismatch closes the NTIRE source path before candidate 2; no
additional protocol successor is allowed.

RAW/JSON/Sony members remain streamed and unpersisted. This is still a
development-only registration preflight, not a colour operator or target
quality experiment, and the bounded candidate counter remains `1/3`.
