# U5.R2BN5 triangular logit transport confirmation result

The unchanged BN4 representation passes the disjoint 64-row AY2 confirmation.
Two reports are byte-identical at `d3a4a1fc...f6f0c7` with stable evidence
`f84d26f4...e4d7d8`; pair-ID and exact-SHA overlap with all BN4 development
populations are both zero.

Oracle improves 37.20% over identity. The source-only adaptive operator improves
10.09% over global, wins 68.75% of rows, has P95/worst ratios of 0.7994/0.8859,
retains 0.9467x AO6 style, and keeps a minimum safe dose of 0.6139. Boundary,
cube, Jacobian-sign and inverse gates all pass without clipping.

This confirms transfer to content untouched by BN4, not camera OOD, film stock,
calibration or user preference. One independent full-resolution visual test may
now compare adaptive versus global before unchanged AO6; all model and gate
choices remain frozen.
