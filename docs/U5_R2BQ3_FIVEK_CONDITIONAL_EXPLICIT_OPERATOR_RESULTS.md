# U5.R2BQ3 bounded conditional explicit operator

BQ3 closes before confirmation. Two formal runs are byte-identical at SHA-256
`41416bb71dca281ab371d852353aa650ef41fb42cbd51c94b0e54711c2c0d765`.
The model uses source pixels only: a development-only 32-component PCA and
closed-form ridge regression predict 14 bounded parameters of one monotone
triangular-logit transport. The full-resolution output remains an analytical
colour operator, never learned RGB.

This experiment supplies useful positive evidence. On held camera groups the
conditional operator improves mean error over the pooled global operator by
4.94% for Aligned Expert and 11.14% for Filtered. It beats the continuous
global-strength control by 2.83% and 7.62%, beats shuffled parameter targets by
6.23% and 9.56%, and has positive group-bootstrap intervals. Filtered also
beats frozen nearest-case retrieval by 5.47%.

It nevertheless fails the frozen contract. Median target-style retention is
only 32.63%/47.30%, showing precisely the averaging failure the anti-bland gate
was designed to expose. New epsilon-boundary fractions reach 0.3906%/0.1953%
against the 0.05% maximum. Aligned also retains only 87.82% of pooled-global
style magnitude and loses to nearest retrieval. The parameter/Jacobian safety
checks themselves are healthy: minimum safe doses are 0.797/0.839, parameter
clipping is sparse, and error tails improve.

The confirmation population was not decoded or scored. No threshold, PCA,
ridge, bound or post-hoc clipping rescue is allowed. The next legal leaf is a
separately frozen factorized direction/magnitude predictor plus analytical
gamut-safe residual execution. It must preserve the same anti-bland and severe
artifact gates. FiveK remains a paired digital-retouch architecture control,
not film response, stock evidence, calibration, preference or product proof.
