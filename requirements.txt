# ============================================================
# K-MCFM 当前 .venv 依赖记录
# Python 3.12.10 + CUDA 12.8 + RTX 5070 Ti Laptop GPU 12GB
# ============================================================

# --- PyTorch (CUDA 12.8) ---
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
torchvision==0.26.0+cu128

# --- Mamba / SSM ---
causal-conv1d==1.6.1
# mamba-ssm 当前未安装；2026-05-06 pip install mamba-ssm 2.3.1 失败，构建环境未找到 nvcc，且 setup.py bare_metal_version 报错

# --- 扩散模型参考 ---
diffusers==0.37.1
accelerate==1.13.0

# --- 视觉骨干 ---
timm==1.0.26

# --- 图像处理 ---
kornia==0.8.2
opencv-python-headless==4.13.0.92
scikit-image==0.26.0
rawpy==0.26.1
pillow==12.1.1

# --- 张量操作 ---
einops==0.8.2
numpy==2.4.4
scipy==1.17.1

# --- KAN ---
pykan==0.2.8
scikit-learn==1.8.0
pandas==3.0.2

# --- 小波变换 (DiMSUM 参考) ---
pytorch-wavelets==1.3.0

# --- 优化 ---
bitsandbytes==0.49.2
xformers==0.0.35

# --- 配置 & 日志 ---
omegaconf==2.3.0
hydra-core==1.3.2
wandb==0.26.1
tensorboard==2.20.0

# --- 训练 ---
pytorch-lightning==2.6.1

# --- 损失函数 & 指标 ---
lpips==0.1.4
piq==0.8.0

# --- 进度条 & 可视化 ---
tqdm==4.67.3
matplotlib==3.10.9

# --- JIT 编译 (可选) ---
ninja==1.13.0

# --- 测试 ---
pytest==9.0.3
pytest-cov==7.1.0
